# Makefile.am for schema/src

# order matters: first collect rncs, then make rngs out of them
SUBDIRS = rnc rng

rncdir=$(schemadir)/autoyast/rnc
rnc_DATA=common.rnc

EXTRA_DIST = \
	     profile.rnc.templ \
	     common.rnc
